---
title: "BIN_Analysis"
output: html_document
date: "2026-06-01"
---
# Habituation Task
## Data Cleaning
```{r}
library(dplyr)
hab <- read.csv("hab.csv")
age <- read.csv("age.csv")
lena <- read.csv("lena.csv")
mcdi <- read.csv("mcdi.csv")

#78 participants (including V1 and V2 data points) with hab data
#this only includes kids who did habituate
hab <- hab %>%
  left_join(age, by = "Participant")

hab <- hab %>%
  left_join(lena, by = "Participant")

#which kids are missing lena data
hab %>%
  filter(is.na(noise_sum)) %>%
  pull(Participant)

#22 kids 
#"M05ES"     "M08SA"     "M43EW"     "M47JJ"     "M48DZ"    
# "M46CC"     "M08SA_V2"  "M24JB_V2"  "B04DR"     "B06JR"     "B10KG"    
#"B17YT"     "B22JR"     "B24EBR"    "B23RF"     "B28APF"    "B29NRP"   
#"B31ZR"     "B34ES"     "B26GB"     "B32NF"     "B12KRM_V2"

hab <- hab %>%
  left_join(mcdi, by = "Participant")

#which kids are missing mcdi data
hab %>%
  filter(is.na(mcdi)) %>%
  pull(Participant)

#31 kids 
#"M11BLP"    "M22AS"     "M41GG"     "M43EW"     "M45DA"     "M47JJ"    
#"M48DZ"     "M46CC"     "M12RO_V2"  "M13CY_V2"  "B05ET"     "B10KG"    
#"B16LR"     "B17YT"     "B18LS"     "B21HM"     "B22JR"     "B24EBR"   
#"B23RF"     "B27KDS"    "B28APF"    "B30MR"     "B29NRP"    "B31ZR"    
#"B34ES"     "B26GB"     "B32NF"     "B33KLH"    "B05ET_V2"  "B11ARF_V2"
#"B12KRM_V2"

# add a language column
hab <- hab %>%
  mutate(
    language = case_when(
      grepl("^M", Participant) ~ "monolingual",
      grepl("^B", Participant) ~ "bilingual",
      TRUE ~ NA_character_
    )
  )
```
## Plotting Novelty Response Score
```{r}
library(ggplot2)

ggplot(hab, aes(x = language, y = nov_score)) +
  geom_boxplot(outlier.shape = NA) +
  geom_jitter(width = 0.1, alpha = 0.7) +
  labs(
    x = "Language Group",
    y = "Novelty Response Score",
    title = "Novelty Response Scores by Language Group"
  ) +
  theme_classic()
```

```{r}
# creating age group bins
hab <- hab %>%
  mutate(
    age_group = case_when(
      age >= 8  & age < 13 ~ "8–12.9",
      age >= 13 & age < 19 ~ "13–18.9",
      age >= 19 & age < 27 ~ "19–26.9",
      TRUE ~ NA_character_
    ),
    age_group = factor(
      age_group,
      levels = c("8–12.9", "13–18.9", "19–26.9")
    )
  )
```

```{r}
ggplot(hab, aes(x = age, y = nov_score, color = language)) +
  geom_point(size = 2, alpha = 0.7) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(
    x = "Age (months)",
    y = "Novelty Response Score",
    color = "Language Group"
  ) +
  theme_classic()
```

```{r}
ggplot(hab, aes(x = age_group, y = nov_score, color = language)) +
  geom_boxplot(position = position_dodge(width = 0.8),
               outlier.shape = NA) +
  geom_jitter(position = position_jitterdodge(
    jitter.width = 0.15,
    dodge.width = 0.8
  ),
  alpha = 0.7) +
  labs(
    x = "Age Group (months)",
    y = "Novelty Response Score",
    color = "Language Group"
  ) +
  theme_classic()

```

## Hab + LENA plots
```{r}
ggplot(hab, aes(x = noise_sum, y = nov_score, color = language)) +
  geom_point(size = 2, alpha = 0.7) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(
    x = "LENA Total Noise",
    y = "Novelty Response Score",
    color = "Language Group"
  ) +
  theme_classic()
```

```{r}
ggplot(hab, aes(x = tv, y = nov_score, color = language)) +
  geom_point(size = 2, alpha = 0.7) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(
    x = "LENA TV Noise",
    y = "Novelty Response Score",
    color = "Language Group"
  ) +
  theme_classic()
```